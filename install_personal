#!/bin/sh

set -euo pipefail

CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"

# We don't want to symlik these
mkdir -vp \
    ~/.config \
    ~/.local \
    "$DATA_HOME" \
    "$CONFIG_HOME/systemd/user/" \
    "$CONFIG_HOME/librewolf" \
    "$CONFIG_HOME/borg"
    

# I want the ~/.local/bin dir to be symlinked to my scripts
[[ -d ~/.local/bin ]] && [[ ! -L ~/.local/bin ]] && mv ~/.local/bin/ ~/.local/bin-backup && echo 'Old ~/.local/bin dir backedup!'

stow -v \
  alacritty \
  applications \
  bkgs \
  borg \
  direnv \
  dunst \
  exiftool \
  fonts \
  gammastep \
  handlr \
  lf \
  librewolf \
  mamba \
  newsboat \
  nvim \
  personal \
  python \
  R \
  scripts \
  shell \
  sounds \
  sway \
  swaynag \
  systemd \
  tmux \
  tofi \
  urlview \
  vivid \
  wget \
  xdg-user-dirs \
  xdg-desktop-portal-wlr \
  zathura

echo 'done!'
