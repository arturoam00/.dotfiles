use_micromamba() {
  [[ -z "$1" ]] && { echo "Usage: use_micromamba <env>"; return 1; }
  eval "$(micromamba shell activate "$1" --shell=bash)"
}

use_venv() {
  VENV=${1:-.venv}
  source "$VENV"/bin/activate
  # [[ -z "$1" ]] && { echo "Usage: use_venv <path/to/activate>"; return 1; }
}
