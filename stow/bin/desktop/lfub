#!/bin/sh

# This is a wrapper script for lf that allows it to create image previews with
# ueberzug. This works in concert with the lf configuration file and the
# lf-cleaner script.

set -e

UB_PID=0
UB_SOCKET=""

case "$(uname -a)" in
    *Darwin*) UEBERZUG_TMP_DIR="${TMPDIR:-/tmp}" ;;
    *) UEBERZUG_TMP_DIR="/tmp" ;;
esac

cleanup() {
    exec 3>&-
    [ -n "$UB_SOCKET" ] && ueberzugpp cmd -s "$UB_SOCKET" -a exit
}

if [ -n "${SSH_CLIENT:-}" ] || [ -n "${SSH_TTY:-}" ] ||
    ! command -v ueberzugpp >/dev/null 2>&1 ||
    ! command -v uuidgen >/dev/null 2>&1
then
    lf "$@"
else
    mkdir -p "${XDG_CACHE_HOME:-$HOME/.cache}/lf"
    UB_PID_FILE="$UEBERZUG_TMP_DIR/.$(uuidgen)"
    ueberzugpp layer --silent --no-stdin --use-escape-codes --pid-file "$UB_PID_FILE"
    UB_PID=$(cat "$UB_PID_FILE")
    rm "$UB_PID_FILE"
    UB_SOCKET="$UEBERZUG_TMP_DIR/ueberzugpp-${UB_PID}.socket"
    export UB_PID UB_SOCKET
    trap cleanup HUP INT QUIT TERM EXIT
    lf "$@" 3>&-
fi
