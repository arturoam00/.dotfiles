# Start Wayland on the first virtual terminal.
if [ -z "${WAYLAND_DISPLAY:-}" ] &&
    [ -n "${XDG_VTNR:-}" ] &&
    [ "$XDG_VTNR" -eq 1 ]
then
    exec sway -d 2&> ~/.sway_log
fi

mkdir -p "$HOME/Docs" "$HOME/Dls" "$HOME/Code" "$HOME/Pix" "$HOME/Vids"
